# Language-detector
This is a command-line tool that detects the language of input text using the Detect Language API. The program accepts text either as command-line arguments or via standard input, sends it to the Detect Language API for analysis, and outputs the detected language(s) along with confidence scores and reliability indicators.
Requires:
    - DETECT_LANGUAGE_API_KEY environment variable to be set
    - detectlanguage package installed
